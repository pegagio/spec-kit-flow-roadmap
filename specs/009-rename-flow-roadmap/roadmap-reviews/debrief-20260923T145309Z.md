# Debrief Report: Rename to FlowKit Roadmap

**Timestamp (UTC)**: 20260923T145309Z
**Roadmap**: `.specify/memory/roadmap.md`
**Verdict**: RETHINK

## Summary

The implementation delta satisfies the tracked identity rename, command and skill naming, configuration migration, dogfood installation, and current-facing documentation requirements. The full test suite passes and the installed payload matches source. Feature acceptance remains blocked by two explicit gates: Linux validation for SC-007 and the GitHub repository/local checkout rename for SC-008. The implementation snapshot was stable and attributable from `HEAD` to `WORKTREE`; this report recommends retaining entry 012 as `in-progress`.

## Target and Selection Provenance

- **Explicit inputs**: `SPEC_TARGET=specs/009-rename-flow-roadmap`; `TARGET=WORKTREE`; no explicit baseline was supplied.
- **Ambient input**: Not used; the active feature pointer was not needed.
- **Selection source**: `explicit-spec`.
- **Match method**: `exact-spec-dir`.
- **Roadmap entry**: 012 — Rename to FlowKit Roadmap.
- **Current status**: `in-progress`.
- **Specification**: `specs/009-rename-flow-roadmap/spec.md`.

## Evidence Boundary

- **Reviewed paths**: `.extensionignore`; `.specify/extensions.yml`; `.specify/extensions/.registry`; `.specify/memory/constitution.md`; `.specify/memory/roadmap.md`; `.specify/extensions/diagram-roadmap/.keep-config`; deleted old payload under `.specify/extensions/diagram-roadmap/`; installed payload under `.specify/extensions/flow-roadmap/`; `CHANGELOG.md`; `ORIGINS.md`; `README.md`; deleted `commands/speckit.diagram-roadmap.*.md`; current `commands/speckit.flow-roadmap.*.md`; `config-template.yml`; `extension.yml`; `scripts/python/load_config.py`; `scripts/python/review_contract.py`; `templates/review-report-template.md`; changed identity, installation, migration, runtime, contract, and protected-history files under `tests/python/`; `tests/python/test_migration.py`; `.agents/skills/speckit-flow-roadmap-*/SKILL.md`; `specs/009-rename-flow-roadmap/spec.md`; `plan.md`; `tasks.md`; `research.md`; `data-model.md`; `quickstart.md`; `contracts/identity-contract.md`; `contracts/migration-contract.md`; `checklists/requirements.md`; and the two pre-implementation brief reports in `roadmap-reviews/`.
- **Unresolved or excluded evidence**: The configured ADR directory `docs/adr` is absent, and no PRD evidence is configured as present. GitHub repository metadata was not queried or changed. The separate Spec Kit Flow bundle and The Diagram are outside this feature's scope.
- **Baseline input / OID**: `HEAD` / `343afb94f4da6909695bd3d5b00b7161474fd125`.
- **Target input / OID**: `WORKTREE` / no immutable target OID.
- **Captured HEAD**: `343afb94f4da6909695bd3d5b00b7161474fd125`.
- **Dirty-state boundary**: 5 staged, 43 unstaged, 25 untracked, and 15 deleted artifacts; no renames or gitlinks. The delta includes the staged feature spec, requirements checklist, and pre-implementation reports present before this debrief.
- **Snapshot identity**: `sha256:668d3736d9f1a1dae5fd6498e45a4f4827a517bcd91c5ff1f08ac6d7165ccba9`; a second capture after evidence collection matched.
- **Material limitations**: Linux checks were unavailable on this macOS host. The local checkout and configured remote still use the prior repository name. These are acceptance gates, not evidence of a defect in the tracked migration procedure.

## Surfaced Context

- **Expected outcome**: FlowKit Roadmap display name, `spec-kit-flow-roadmap` project identity, `flow-roadmap` extension ID, four `speckit.flow-roadmap.*` commands, four `speckit-flow-roadmap-*` skills, preserved purpose behavior, and documented manual configuration migration.
- **Scope in/out**: Align current source, package, dogfood installation, generated skills, tests, and documentation; preserve accepted history. Changes to the separate Spec Kit Flow bundle and The Diagram are out of scope.
- **Dependencies and states**: 008 (`verified`), 009 (`verified`), and 010 (`verified`).
- **Governing decisions**: C-01, C-03, C-05, C-07, and C-08; no resolved ADR was available.
- **Addresses**: No PRD pointer is recorded.
- **Open questions and cross-cutting notes**: The intended user selected a documented manual migration that preserves configuration. The external rename is separately coordinated; no roadmap status change was approved or applied in this report.

## Findings

| ID | Severity | Category | Finding | Evidence | Suggestion |
|---|---|---|---|---|---|
| F1 | Must-Address | outcome-miss | The configured remote URL and local checkout basename still use `speckit-diagram-roadmap`, so FR-012 and SC-008 are not satisfied. | `spec.md` (FR-012, SC-008); `tasks.md` (T034); `git remote -v`; `git worktree list --porcelain` | Coordinate the GitHub and local checkout/worktree rename operations, then verify the repository name, all checkout basenames, remote URL, and extension links. |
| F2 | Must-Address | outcome-miss | Required Linux validation for SC-007 has not run; this host is macOS and has no active Linux VM. | `spec.md` (SC-007); `tasks.md` (T031); `quickstart.md`; local `colima status` and `limactl list` | Run the quickstart clean-install and migration checks on Linux and retain the results before claiming platform acceptance. |

## Findings Summary

**Configured cap**: 50
**Total / displayed / omitted**: 2 / 2 / 0

| Dimension | Value | Total | Displayed | Omitted |
|---|---|---:|---:|---:|
| Severity | Must-Address | 2 | 2 | 0 |
| Severity | Recommendation | 0 | 0 | 0 |
| Severity | Question | 0 | 0 | 0 |
| Category | outcome-miss | 2 | 2 | 0 |

## Lifecycle Recommendation

- **Current status**: `in-progress`.
- **Proposed status**: None.
- **Gate results**: Eligible source status: yes; outcome met: no; delta trustworthy: yes; Must-Address findings clear: no.
- **Approval required**: No approval is needed to keep the current status; this report applies no status change.

## Recommended Actions

1. **F1**: Complete and verify the separately coordinated repository and checkout rename before accepting entry 012.
2. **F2**: Run and record Linux validation before accepting cross-platform support.
3. Keep entry 012 `in-progress` until both acceptance gates pass; any roadmap change must go through `speckit.flow-roadmap.write` with explicit authorization.

This report preserves source and governance artifacts. Its creation is the debrief command's sole permitted write; it proposes changes but never applies them.

*Generated by `speckit.flow-roadmap.debrief`.*
