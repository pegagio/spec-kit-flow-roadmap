# Debrief Report: Rename to FlowKit Roadmap

**Timestamp (UTC)**: 20260923T150113Z
**Roadmap**: `.specify/memory/roadmap.md`
**Verdict**: RETHINK

## Summary

The tracked identity rename, command and skill naming, configuration migration, dogfood installation, and current-facing documentation meet the feature's requirements. The required macOS checks have passed. Linux remains a supported runtime platform, but the intended user clarified that Linux execution is optional for this feature's acceptance. Acceptance remains blocked only by the external GitHub repository and local checkout/worktree rename required by FR-012 and SC-008. Entry 012 should remain `in-progress`.

This report supersedes the earlier debrief's Linux finding in `debrief-20260923T145309Z.md`. That earlier report records the criteria before the user's clarification; it remains unchanged as dated evidence.

## Target and Selection Provenance

- **Explicit inputs**: `SPEC_TARGET=specs/009-rename-flow-roadmap`; `TARGET=WORKTREE`; no explicit baseline was supplied.
- **Ambient input**: Not used; the active feature pointer was not needed.
- **Selection source**: `explicit-spec`.
- **Match method**: `exact-spec-dir`.
- **Roadmap entry**: 012 — Rename to FlowKit Roadmap.
- **Current status**: `in-progress`.
- **Specification**: `specs/009-rename-flow-roadmap/spec.md`.

## Evidence Boundary

- **Reviewed paths**: `.extensionignore`; `.specify/extensions.yml`; `.specify/extensions/.registry`; `.specify/memory/constitution.md`; `.specify/memory/roadmap.md`; retained old config and removed old payload under `.specify/extensions/diagram-roadmap/`; installed payload under `.specify/extensions/flow-roadmap/`; `CHANGELOG.md`; `ORIGINS.md`; `README.md`; old and renamed command files; `config-template.yml`; `extension.yml`; both Python runtime scripts; review and roadmap templates; changed files under `tests/python/`; all four `.agents/skills/speckit-flow-roadmap-*/SKILL.md` files; `spec.md`; `plan.md`; `tasks.md`; `research.md`; `data-model.md`; `quickstart.md`; both contracts; the requirements checklist; both pre-implementation brief reports; `debrief-20260923T145309Z.md`; and the project-wide sync report.
- **Unresolved or excluded evidence**: The configured ADR directory `docs/adr` is absent, and no PRD evidence is configured as present. GitHub repository metadata was not queried or changed. The separate Spec Kit Flow bundle and The Diagram are outside this feature's scope.
- **Baseline input / OID**: `HEAD` / `343afb94f4da6909695bd3d5b00b7161474fd125`.
- **Target input / OID**: `WORKTREE` / no immutable target OID.
- **Captured HEAD**: `343afb94f4da6909695bd3d5b00b7161474fd125`.
- **Dirty-state boundary**: 5 staged, 44 unstaged, 27 untracked, and 15 deleted artifacts; no renames or gitlinks. The delta includes staged feature artifacts and prior review reports.
- **Snapshot identity**: `sha256:5d18e882aa94e65da9cb45d3ca352c0a17cba1fcdfdf2f76c7a7741912778434`; a second capture after evidence collection matched.
- **Material limitations**: The remote and checkout names remain unchanged pending the separate coordination required by T034. No GitHub rename operation was attempted.

## Surfaced Context

- **Expected outcome**: FlowKit Roadmap display name, `spec-kit-flow-roadmap` project identity, `flow-roadmap` extension ID, four `speckit.flow-roadmap.*` commands, four `speckit-flow-roadmap-*` skills, preserved purpose behavior, and documented manual configuration migration.
- **Scope in/out**: Align current source, package, dogfood installation, generated skills, tests, and documentation; preserve accepted history. Linux runtime support remains required, while Linux validation for this feature is optional per the user's clarification. Changes to the separate Spec Kit Flow bundle and The Diagram are out of scope.
- **Dependencies and states**: 008 (`verified`), 009 (`verified`), and 010 (`verified`).
- **Governing decisions**: C-01, C-03, C-05, C-07, and C-08; no resolved ADR was available.
- **Addresses**: No PRD pointer is recorded.
- **Open questions and cross-cutting notes**: The user selected a documented manual migration that preserves configuration and clarified that Linux execution is not required for acceptance. No roadmap status change was approved or applied in this report.

## Findings

| ID | Severity | Category | Finding | Evidence | Suggestion |
|---|---|---|---|---|---|
| F1 | Must-Address | outcome-miss | The GitHub repository and local checkout/worktree basenames still use `speckit-diagram-roadmap`, so FR-012 and SC-008 remain unsatisfied. | `spec.md` (FR-012, SC-008); `tasks.md` (T034); `git remote get-url origin`; `git worktree list --porcelain` | Coordinate the separately authorized GitHub and local checkout/worktree rename operations, then verify the repository name, every checkout basename, remote URL, and extension links. |

## Findings Summary

**Configured cap**: 50
**Total / displayed / omitted**: 1 / 1 / 0

| Dimension | Value | Total | Displayed | Omitted |
|---|---|---:|---:|---:|
| Severity | Must-Address | 1 | 1 | 0 |
| Severity | Recommendation | 0 | 0 | 0 |
| Severity | Question | 0 | 0 | 0 |
| Category | outcome-miss | 1 | 1 | 0 |

## Lifecycle Recommendation

- **Current status**: `in-progress`.
- **Proposed status**: None.
- **Gate results**: Eligible source status: yes; outcome met: no; delta trustworthy: yes; Must-Address findings clear: no.
- **Approval required**: No approval is needed to keep the current status; this report applies no status change.

## Recommended Actions

1. **F1**: Complete and verify the separately coordinated GitHub repository and local checkout/worktree rename before accepting entry 012.
2. Keep entry 012 `in-progress` until SC-008 passes; route any roadmap change through `speckit.flow-roadmap.write` with explicit authorization.

This report preserves source and governance artifacts. Its creation is the debrief command's sole permitted write; it proposes changes but never applies them.

*Generated by `speckit.flow-roadmap.debrief`.*
