# Minimal config — used together with SPECKIT_DIAGRAM_ROADMAP_* env var overrides in tests.
# Only sets one key to confirm env always wins over file values.
roadmap:
  path: "docs/env-test-roadmap.md"
